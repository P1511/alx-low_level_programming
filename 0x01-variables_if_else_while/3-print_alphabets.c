#include <stdio.h>
/**
*main - the entry point
*conditions
*Return: Always 0
*/
int main(void)
{
	char letter = 'A';
	char letter = 'a';
	while (letter <= 'z')
	{
		putchar(letter);
		letter++;
	}
	while (letter <= 'Z')
	{
		putchar(letter);
		letter++;
	}
		puchar('n\');
	return (0);
}
